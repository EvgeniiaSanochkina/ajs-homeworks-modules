import defaultGame, { readGameSaving as loadGame, writeGameSaving as saveGame } from './game.js';

//console.log(defaultGame, loadGame, saveGame)
//const game = new defaultGame();

defaultGame.start();

//console.log(loadGame);